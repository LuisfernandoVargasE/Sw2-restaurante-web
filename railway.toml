[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["nodejs_22", "php", "composer"]

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "php artisan migrate --force && php artisan serve --host 0.0.0.0 --port $PORT"
